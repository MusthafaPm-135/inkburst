# Use your Render backend URL when deploying the frontend to Vercel.
VITE_API_URL=https://your-render-service.onrender.com/api

# Create a Tawk.to property, then copy these values from Administration → Chat Widget.
VITE_TAWK_PROPERTY_ID=your_tawk_property_id
VITE_TAWK_WIDGET_ID=your_tawk_widget_id
